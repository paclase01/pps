name: gitleaks

on: [push,pull_request]

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: pps
      uses: paclase01/pps@master
        with:
          config-path: .gitleaks.yml
